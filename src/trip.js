// class Trip {
   constructor(newTrip) {
     this.id = trip.id

   }
 

// methods
// getDestinationByID()- returns destanation info by ID
// calculateTripCost() - calculate the total cost of the trip, including lodging and flight and returns the cost. Will need # days, #travelers, and destination
// 